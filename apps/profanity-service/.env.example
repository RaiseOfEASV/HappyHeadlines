POSTGRES_DB=profanity_db
POSTGRES_USER=profanity_user
POSTGRES_PASSWORD=change_me_before_use

# Seq ingestion API key – create in Seq UI after the observability stack is running:
# Seq → Settings → API Keys → Add API Key (Ingest) → paste token below
SEQ_API_KEY=
